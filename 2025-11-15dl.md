# 深度学习/实习学习日志

**日期**：2025年11月14日
**主题**：前馈网络 FFN、隐式/显式卷积、门控机制 Gating 与 Jupyter vs VS Code 对比

---

## 今天学/记录到的要点

* **前馈网络 (FFN)**

  * 是 Transformer 每层中除注意力外的关键组成，等价于一个两层 MLP：
    [
    \text{FFN}(x)=W_2 \sigma(W_1x+b_1)+b_2
    ]
  * 对每个 token 独立地做非线性变换，不改变序列关系。
  * 常见设置：中间层维度为输入维度 4×，激活函数 GELU 或 ReLU。

* **显式 vs 隐式卷积**

  * **显式卷积**：像 CNN 那样直接滑动卷积核，计算 O(L × K)，当 K 很大时成本高。
  * **隐式卷积**：卷积核不显式存储，而由参数化函数或 FFT 在频域生成，可处理上百万长度序列，复杂度 O(L log L)。
  * **Hyena / StripedHyena2** 正是利用隐式长卷积替代大部分自注意力层。

* **门控 (Gating)**

  * 控制信息流动的“阀门”，学习哪些信号通过、哪些被抑制。
  * 常见形式：(y=g(x)\odot f(x))，其中 g(x) 用 sigmoid 输出 0–1 权重。
  * 在 Hyena 层中，门控决定何时依赖长程卷积上下文，何时保留局部特征。

* **卷积 + 轻量注意力 = 混合架构优势**

  * 传统 Transformer 的自注意力是 O(L²)，难以扩展到百万序列。
  * 混合架构把大部分工作交给卷积，保留少量注意力层处理关键依赖，效率更高。
  * 在 Evo 2 中正是用 StripedHyena 2 这种结构支撑 1 M 上下文。

* **Jupyter Notebook vs VS Code 的角色**

  * **Jupyter** = 实验台：交互、可视化、快速试错、科研记录。
  * **VS Code** = 工厂：模块化、调试、版本控制、部署。
  * 专业流程通常是“在 Notebook 里原型验证 → 在 VS Code 中重构与训练 → 在服务器部署”。

---

## 小结

1. **FFN 让每个 token 自己“思考”**，是注意力后的非线性映射核心。
2. **隐式卷积 + 门控 机制** 在超长序列建模中替代了笨重的全注意力，计算更快更稳。
3. **Jupyter 与 VS Code 互补**：前者用于探索，后者用于构建与落地。

---

## 今日自问自答

**Q：为什么 FFN 要扩大维度再缩回？**
A：中间更高维可以提供非线性组合空间，让模型捕获更复杂特征，再压回原维度方便残差连接。

**Q：隐式卷积相比显式卷积具体优势是什么？**
A：通过频域表示 (FFT) 或函数生成权重，能在相同算力下覆盖更长依赖范围，显著降低显存与计算量。

**Q：门控 (Gating) 的数学意义？**
A：是一种动态加权机制，输出 = 主分支 × 门值；网络可自适应选择信号路径，提高表示灵活性。

**Q：为什么深度学习研究多用 Jupyter 而工程部署多用 VS Code？**
A：Notebook 方便实验与展示，而 VS Code 利于代码组织、版本管理与生产环境调试。

---

## 下一步计划

1. 画出 Transformer 层、Hyena 层、Hybrid 层的结构对比图。
2. 复现 Hyena 隐式卷积的 FFT 实现，比较与标准 Conv1d 速度差异。
3. 在 Jupyter 中实现门控线性单元 (GLU) 示例并可视化门值变化。
4. 用 VS Code 重构当前实验代码，学习如何将 .ipynb 转 .py 模块化管理。

另外，今天阅读了一些关于生物大模型方向的前沿论文，请教了肖哥，明确了如果想做一些深度学习上较为深度的研究，我需要的时间、人员、技术、成本。
