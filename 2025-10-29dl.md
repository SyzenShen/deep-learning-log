# 深度学习/实习学习日志

**日期**：2025年10月28日
**主题**：计算图、自动求导与最小神经网络示例（y = x²）

---

## 今天学/记录到的要点

* **Tensor 是一切的基础**：可以理解为多维数组（向量→矩阵→高维张量），是 PyTorch 操作数据的最小单位。
  常见形状含义：

  * `[N]` → 一维向量
  * `[N, M]` → 二维矩阵
  * `[B, C, H, W]` → 图像批量输入（Batch × Channel × Height × Width）

* **CUDA = GPU 的计算平台**：`.cuda()` 或 `.to(device)` 是把张量送进显卡显存里运算。GPU 的优势是可以并行进行成千上万个矩阵乘法。

* **requires_grad=True**：告诉 PyTorch “我要学习这个变量”。系统会记录所有依赖关系，形成一张**计算图 (Computational Graph)**。

* **.backward() = 自动求导**：当损失 `loss` 调用 `.backward()` 时，PyTorch 会沿计算图反向传播梯度，自动计算出每个参数的偏导（∂loss/∂param）。

* **训练流程的四步循环**：

  1. `forward`：前向传播，计算预测输出
  2. `loss`：计算损失函数
  3. `backward`：反向传播，计算梯度
  4. `optimizer.step()`：更新参数
     搭配 `optimizer.zero_grad()` 清理上次梯度，循环迭代直到收敛。

* **最小神经网络示例（y = x²）**：
  用 2 层线性层 + ReLU 激活函数的模型成功拟合 y = x² 函数。训练 500 epoch 后，Loss 从 19 → 0.0067，说明即便是简单的网络，也能在自动求导与梯度更新下学会非线性关系。

---

## 小结（三句话）

1. **计算图是 PyTorch 的灵魂**：它像一张“数学电路图”，记录每一步的运算依赖，方便自动反向传播。
2. **.backward() 让梯度流动起来**：它不只是算偏导，而是驱动整个学习过程。
3. **看似简单的训练循环，其实是 AI 的全部基础**：forward → backward → update，这三步构成了所有神经网络的最小闭环。

---

## 今日自问自答（反思）

**Q：为什么需要 `requires_grad=True`？**
A：因为只有被标记的张量才会进入计算图。否则 PyTorch 不会浪费资源去追踪不需要学习的变量。

**Q：`.backward()` 实际做了什么？**
A：沿计算图从输出往回走，根据链式法则（∂z/∂x = ∂z/∂y × ∂y/∂x）层层计算梯度，并写入每个参数的 `.grad`。

**Q：为什么训练前要 `optimizer.zero_grad()`？**
A：PyTorch 的梯度是**累积的**。如果不清零，会把上一次的梯度继续叠加，导致更新方向错误。

**Q：为什么 print(y)？**
A：只是为了查看中间结果，验证矩阵乘法是否正确，并看到计算图是否生成（会显示 `grad_fn=<MmBackward0>`）。

---

## 下一步计划

1. 用 Matplotlib 可视化 y = x² 的拟合曲线（对比真实曲线与模型预测）。
2. 在此基础上实现更复杂函数（如 y = sin(x)），体会模型容量差异。
3. 复盘 `.backward()` 的链式求导逻辑，画出计算图节点流向。
4. 尝试用 GPU 加速训练，测量 CPU vs CUDA 的性能差异。

